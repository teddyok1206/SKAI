# SKAI Agent Entry Point

The canonical project agent instructions live in `AGENT.md`.

Follow `AGENT.md` for all planning, implementation, documentation, git, deployment, and SKAI philosophy checks. In particular, judge/coaching development must follow the `.skai fixture -> derived extension -> unified viewer overlay -> regression` loop documented there.

This file exists because some tools look for `AGENTS.md`; do not duplicate the full rules here.

