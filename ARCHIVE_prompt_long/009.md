# 009

Date: 2026-06-02

## Prompt

SKAI system, background prompt가 사용자 환경에서 왜 보여...? 이건 절대 보이면 안 되지. 그리고 애초에 보내져서도 안 돼 사용자의 프롬프트에 응답하는 gemini는 진짜 아무것도 몰라야 함. 다만 우리가 context 조절은 직접 해주는 거고 (breakpoint, branch 등등 관리 및 구현하기 위해). 그리고 렌더링 매우 잘됨.

## Captured Product Correction

The prior context transparency implementation was useful for diagnosis but incorrect for the product surface and runtime contract.

Correct rule:

- The user-facing problem-solving model must not receive SKAI system prompt or hidden problem background.
- The user-facing model should receive only the visible attempt trace selected by SKAI plus files/materials the user explicitly attaches.
- SKAI may manage context externally for breakpoint, branch, pruning, replay, judging, and research storage.
- Internal judge/counterfactual flows may still receive rubric/context prompts because they are not the user's live problem-solving model.
- Markdown/LaTeX rendering is correct and should remain.
